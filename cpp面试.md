# C++ 面试整理

## 1. 指针和引用有什么区别？

**回答：**  
指针保存的是某个对象的地址；引用是某个对象的别名。指针可以为空、可以重新指向别的地址；引用一旦绑定就不能改绑，而且通常不能为空。

## 2. const 关键字怎么理解？

**回答：**  
`const` 的核心是限定“不能修改”。它可以修饰变量、指针、引用和成员函数。最常见的是区分“指针本身不能变”还是“指针指向的内容不能变”。

- `const int *p`：指向常量的指针
- `int * const p`：常量指针
- `const int * const p`：指向常量的常量指针

## 3. new/delete 和 malloc/free 有什么区别？

**回答：**  
`new/delete` 是 C++ 的内存申请和释放方式，会调用构造函数和析构函数；`malloc/free` 是 C 语言的内存管理函数，只负责分配和释放内存，不会调用构造或析构。

区别主要有：
- `new` 返回对应类型指针，`malloc` 返回 `void*`
- `new/delete` 支持对象生命周期管理，`malloc/free` 不支持
- `new` 可重载，`malloc` 不能重载

## 4. 构造函数和析构函数分别什么时候调用？

**回答：**  
构造函数在对象创建时自动调用，用来完成初始化；析构函数在对象生命周期结束时自动调用，用来清理资源。

## 5. 拷贝构造函数和赋值运算符有什么区别？

**回答：**  
拷贝构造函数是在**新对象创建时**，用一个已有对象初始化当前对象；赋值运算符是在**对象已经存在时**，把一个已有对象的值赋给当前对象。

## 6. 什么是深拷贝和浅拷贝？

**回答：**  
浅拷贝只是复制指针本身，不复制指针指向的内容；深拷贝会重新开辟内存，并把内容也复制过去。  
如果类里管理了堆内存，通常要做深拷贝，否则可能出现重复释放问题。

## 7. 什么是虚函数？为什么要用虚函数？

**回答：**  
虚函数是允许子类重写、并通过基类指针或引用实现动态绑定的成员函数。它的作用是支持运行时多态，让调用哪个函数由对象的真实类型决定，而不是由指针或引用的静态类型决定。

## 8. 什么是纯虚函数和抽象类？

**回答：**  
纯虚函数是没有具体实现、声明为 `= 0` 的虚函数。  
包含纯虚函数的类叫抽象类，抽象类不能直接实例化，通常用于定义接口或基类规范。

## 9. 虚析构函数为什么要有？

**回答：**  
如果基类指针指向派生类对象，并且通过基类指针删除对象，那么基类析构函数必须是虚函数，否则只会调用基类析构，导致派生类资源无法正确释放。

## 10. 什么是智能指针？你了解哪些智能指针？

**回答：**  
智能指针是 RAII 思想的封装，用对象管理动态内存，避免手动 `new/delete` 带来的泄漏问题。常见的有：
- `std::unique_ptr`
- `std::shared_ptr`
- `std::weak_ptr`

## 11. shared_ptr 的引用计数是怎么工作的？shared_ptr 的核心原理

**回答：**  
`shared_ptr` 通过引用计数管理资源。每复制一个 `shared_ptr`，引用计数加 1；每销毁一个 `shared_ptr`，引用计数减 1；当计数变为 0 时，自动释放所管理的对象。  
它的核心原理通常是：控制块 + 引用计数 + 自定义删除器等。

## 12. 什么是 RAII？为什么 C++ 里很重要？

**回答：**  
RAII 是“资源获取即初始化”的思想。它把资源的申请和释放绑定到对象的构造和析构上，通过对象生命周期自动管理资源。  
它很重要，因为这样可以减少内存泄漏、异常安全问题和资源遗漏释放的问题。

## 13. weak_ptr 是怎么解决 shared_ptr 循环引用问题的？

**回答：**  
`weak_ptr` 不增加引用计数，只观察资源不拥有资源。  
当两个对象互相持有 `shared_ptr` 时，会导致引用计数永远不为 0，从而内存泄漏。把其中一方改成 `weak_ptr` 后，就不会形成强引用环，可以正常释放。

## 14. 什么是 RAII？为什么它很重要？

**回答：**  
RAII 是“资源获取即初始化”的思想。资源在对象构造时获取，在对象析构时释放。它很重要，因为能让资源管理自动化、异常安全化，避免手动释放资源出错。

## 15. 什么是右值引用？为什么要引入它？

**回答：**  
右值引用是绑定到临时对象或将亡值上的引用，写作 `T&&`。  
引入它主要是为了支持移动语义和完美转发，减少不必要的拷贝，提高性能。

## 16. 什么是移动构造和移动赋值？

**回答：**  
移动构造函数和移动赋值运算符不是复制资源，而是“转移资源所有权”。它们会把临时对象中的资源直接转移给当前对象，避免深拷贝，提高效率。

## 17. 什么是完美转发？

**回答：**  
完美转发是指在模板中，把参数的原始左值/右值属性完整地传递给下游函数。通常通过 `std::forward` 实现。

## 18. vector 的扩容机制是怎样的？

**回答：**  
`vector` 的底层是连续内存数组。当容量不够时，会重新申请更大的连续空间，把原来的元素拷贝或移动过去，再释放旧内存。  
扩容通常是按倍数增长，以减少频繁重新分配带来的开销。

## 19. map 和 unordered_map 有什么区别？

**回答：**  
`map` 底层通常是红黑树，元素有序，查找、插入、删除复杂度是 `O(log n)`。  
`unordered_map` 底层是哈希表，元素无序，平均查找、插入、删除复杂度是 `O(1)`，但最坏情况可能退化。

## 20. unordered_map 的哈希冲突怎么处理？

**回答：**  
`unordered_map` 通常用拉链法处理哈希冲突，也就是同一个桶里挂多个元素。发生冲突时，会把元素放到对应桶的链表或类似结构中。

## 21. 什么是负载因子？为什么要扩容？

**回答：**  
负载因子通常是“元素个数 / 桶的数量”。当负载因子过高时，哈希冲突会增多，查询效率下降，所以需要扩容，重新哈希分配元素，降低冲突概率，提高性能。

## 22. 什么是 lambda？

**回答：**  
lambda 是一种匿名函数，可以就地定义、就地使用，适合短小的回调和局部逻辑。它本质上是一个闭包对象，可以捕获外部变量。

## 23. lambda 可以转换成函数指针吗？

**回答：**  
只有**不捕获任何外部变量**的 lambda 才可以隐式转换成函数指针。  
如果捕获了外部变量，它就变成闭包对象，不能直接转换成普通函数指针。

## 24. 什么是泛型 lambda？

**回答：**  
泛型 lambda 是指在 lambda 参数里使用 `auto`，让它可以接受不同类型的参数。它是 C++14 引入的特性，本质上相当于带模板 `operator()` 的闭包对象。

## 25. 什么是线程？线程和进程有什么区别？

**回答：**  
线程是 CPU 调度的最小单位，进程是资源分配的基本单位。一个进程可以包含多个线程，线程共享进程的内存和资源，但各自有独立的执行上下文。线程切换比进程切换开销更小。

## 26. 什么是线程安全？

**回答：**  
线程安全是指多个线程并发访问共享资源时，程序仍能保持正确行为，不会出现数据竞争和错误结果。通常通过锁、原子操作等方式实现。

## 27. 什么是互斥锁 mutex？

**回答：**  
互斥锁是一种同步原语，用来保护共享资源，保证同一时刻只有一个线程进入临界区。它的主要作用是避免并发访问导致的数据竞争，而不是防止死锁。

## 28. 什么是死锁？怎么避免死锁？

**回答：**  
死锁是指多个线程因互相等待对方持有的资源，而都无法继续执行的状态。常见的死锁场景是多个线程以不同顺序申请多把锁，导致彼此都在等待。  
避免死锁的方法包括：固定加锁顺序、一次只申请一把锁、使用 `std::lock`、减少锁的嵌套、用 `try_lock` 尽量避免长时间等待。

## 29. `lock_guard` 和 `unique_lock` 有什么区别？

**回答：**  
`lock_guard` 是最简单的锁管理类，构造时自动加锁，析构时自动解锁，不能手动解锁，开销小，适合简单场景。  
`unique_lock` 功能更强，除了自动加锁/解锁，还支持手动 `lock()`、`unlock()`、延迟加锁、配合条件变量使用，但开销比 `lock_guard` 稍大。

## 30. 什么是条件变量 `condition_variable`？

**回答：**  
条件变量是用于线程间同步的一种机制，常和互斥锁一起使用。它允许一个线程等待某个条件成立，直到另一个线程通知它继续执行。常见场景是生产者消费者模型。

## 31. 为什么条件变量要配合 `mutex` 使用？

**回答：**  
条件变量配合互斥锁使用，是为了保护共享状态并避免竞态条件。线程在检查条件和进入等待之间，如果没有锁保护，可能会错过通知或读到不一致的状态。所以通常要先加锁，再检查条件，然后在条件不满足时调用 `wait` 进入等待。
